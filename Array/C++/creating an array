// array declaration by initializing values

int arr[] = {2,3,4,5}
// This creates an array with 5 elements.


//creating an array with a specific no. of elements and partially filling it.

int arr[5] = {1,3,5}
//This will create an array of size 5 where only 3 are given the values , you can later insert the value for the rest 2.


//indivially assinging a value. 

int arr[3];
cin>>arr[0]>>arr[1]>>arr[2];
//This will make an array of size 3 and we would take the inputs individually from the user.


//taking input using loop

int arr[7];
for(int i=0; i<7; i++){
  cin>>a[i];
  }
// this will create an array of size 7 and take input from the user.
